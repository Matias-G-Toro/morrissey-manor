media: media
content:
  - name: html-pages
    label: HTML Pages
    type: collection
    path: docs
    fields:
      - name: title
        type: string
      - name: source_code
        type: code
        options:
          format: html
